---
description: Obligato status — current task, stage, budget state, pinned lockfile (UX §3)
allowed-tools: Bash(bun packages/cc-plugin/src/status.ts)
---

Run `bun packages/cc-plugin/src/status.ts` from the repo root and show the user its output verbatim in a fenced code block. Add nothing else unless the output itself indicates a problem (e.g. no sessions recorded), in which case add one line explaining the likely cause.
